#git diff HEAD --quiet || { echo 'You have unsaved changes in your git'; exit 1; }
npm run prettier || { echo 'Failed when formatting the codes'; exit 1; }
npm run build || { echo 'Error when transpiling the project to JS'; exit 1; }